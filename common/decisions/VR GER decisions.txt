Vanillaredux_GCW_cat = {
	GCW_push_against_nazis = {
		targets = { D04 }
		available = {
			has_war = yes
			is_neighbor_of = D04
		}
		cost = 30
		ai_will_do = {
			factor = 135
		}
		targeted_modifier = {
			tag = D04
			attack_bonus_against = 0.2
			breakthrough_bonus_against = 0.15
			defense_bonus_against = -0.15
		}
		days_remove = 50
		days_re_enable = 15
		
 	}
	GCW_push_against_junta = {
		targets = { GER }
		available = {
			has_war = yes
			is_neighbor_of = GER
		}
		cost = 30
		ai_will_do = {
			factor = 135
		}
		targeted_modifier = {
			tag = GER
			attack_bonus_against = 0.2
			breakthrough_bonus_against = 0.15
			defense_bonus_against = -0.15
		}
		days_remove = 50
		days_re_enable = 15
		
 	}
	GCW_push_against_democrats = {
		targets = { WGR }
		available = {
			has_war = yes
			is_neighbor_of = WGR
		}
		ai_will_do = {
			factor = 135
		}
		cost = 30
		targeted_modifier = {
			tag = WGR
			attack_bonus_against = 0.2
			breakthrough_bonus_against = 0.15
			defense_bonus_against = -0.15
		}
		days_remove = 50
		days_re_enable = 15
		
 	}
	GCW_push_against_commies = {
		targets = { RHI }
		available = {
			has_war = yes
			is_neighbor_of = RHI
		}
		cost = 30
		ai_will_do = {
			factor = 135
		}
		targeted_modifier = {
			tag = RHI
			attack_bonus_against = 0.2
			breakthrough_bonus_against = 0.15
			defense_bonus_against = -0.15
		}
		days_remove = 50
		days_re_enable = 15
		
 	}
}

operations = {
	strike_american_warlords = {
		name = "Strike [FROM.GetNameDef]"
		war_with_target_on_complete = yes
		targets = { CAL UTS ORS JES WAS AZS NVJ CSC }
		allowed = {
			OR = {
				tag = CAL
				tag = TEX
				tag = USA
				tag = KSS
			}
		}
		fire_only_once = yes
		available = {
			OR = {
				all_enemy_country = { NOT = { is_neighbor_of = ROOT } }
				has_war = no
			}
			is_neighbor_of = FROM
			NOT = { has_country_flag = declare_war_preparing }
		}
		cost = 25
		days_remove = 20
		ai_will_do = {
			base = 20
			modifier = {
			add = 80
				strength_ratio = {
				tag = FROM
				ratio > 0.85
				}
				ic_ratio = {
				tag = FROM
				ratio > 0.85
				}
			}
			modifier = {
			add = 60
				strength_ratio = {
				tag = FROM
				ratio > 0.8
				}
				ic_ratio = {
				tag = FROM
				ratio > 0.8
				}
			}
		}
		complete_effect = { set_country_flag = declare_war_preparing }
		cancel_effect = { clr_country_flag = declare_war_preparing }
		remove_effect = {
			declare_war_on = {
   				target = FROM
    				type = annex_everything
			}
			clr_country_flag = declare_war_preparing
		}
	}
}